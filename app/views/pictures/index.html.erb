
  <fieldset>
    <legend>Gallery</legend>
  </fieldset>
<div class="col-md-12">
	<% @pictures.each do |picture| %>
	<div class="col-md-3">
		<div class="img-wrapper">
			<%= link_to theme_picture_path(picture.theme_id, picture.id) do %>
			<div class="imgbox">
				<img src="<%= picture.url%>">
				<span class="caption fade-caption">
					<h3><%= picture.name%></h3>
					<p>Text den du haben möchtest</p>
				</span>
			</div>
			<% end %>
		</div>
	</div>
	<% end %>
</div>


